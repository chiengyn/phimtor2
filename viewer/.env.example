# --- Viewer server (public) ---
# Read-only against the catalog. The only tables it writes are `users`,
# `user_bookmarks` (accounts + the saved list) and the billing tables, and it
# never runs migrations — the admin service owns the schema.
VIEWER_PORT=8082

# Support Discord invite link, surfaced in the site header/footer.
# Leave empty to hide the link.
VIEWER_DISCORD_URL=https://discord.gg/PhaE6DgnC

# --- Google sign-in (OAuth2 / OIDC) ---
# Create a "Web application" OAuth client at
# https://console.cloud.google.com/apis/credentials and register these EXACT
# authorized redirect URIs (byte for byte — a mismatch is the usual failure):
#   http://localhost:8082/auth/google/callback   (local dev)
#   https://phimnet.online/auth/google/callback  (production)
# The URI is derived from VIEWER_PUBLIC_URL, so there is no separate env var.
# Leave these empty to disable accounts entirely: the /auth routes are then not
# registered and the header shows no login button, i.e. the site behaves exactly
# as it did before accounts existed.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# HMAC key for the signed session cookie. There is no sessions table — the cookie
# IS the session — so rotating this logs everyone out, which is also the only
# revocation lever. Generate with:
#   openssl rand -hex 32
# REQUIRED once VIEWER_PUBLIC_URL is set (a production-shaped config). Left unset
# locally, the viewer generates an ephemeral key at boot and sessions die on
# restart.
SESSION_SECRET=

# --- MySQL (shared with the admin service) ---
# Either set the individual DB_* values or a full MYSQL_DSN (which overrides them).
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=phimtor
DB_PASSWORD=phimtor
DB_NAME=phimtor
# MYSQL_DSN=phimtor:phimtor@tcp(127.0.0.1:3306)/phimtor?parseTime=true&charset=utf8mb4

# --- Streamer manager (control plane) ---
# The viewer adds torrents server-to-server via the manager (internal URL + bearer
# token). The manager picks a streamer and returns its public URL, which the
# prepare response hands to the browser for stats + stream directly — so there is
# no single static public streamer URL anymore. MANAGER_INTERNAL_TOKEN must match
# the manager's MANAGER_INTERNAL_TOKEN; empty disables it.
MANAGER_INTERNAL_URL=http://localhost:8083
MANAGER_INTERNAL_TOKEN=

# Seconds a watch session may go silent before the viewer treats the tab as gone
# and drops its torrent (via the manager) to free streamer resources. The watch
# page also beacons a leave on page hide for an instant drop; this TTL is the
# backstop for crashed tabs / dead networks. Keep it well above the page's 10s
# heartbeat interval so a couple of missed beats don't evict an active viewer.
WATCH_HEARTBEAT_TTL=30

# --- Paid 4K tier (crypto) ---
# Leave ALL of these empty to disable billing: localized plan/payment pages are then not
# registered and a 4K source shows "sắp ra mắt" for everyone, exactly as the site
# behaved before billing existed. That is the clean rollback.
#
# Billing also requires GOOGLE_CLIENT_ID/SECRET above: an entitlement hangs off a
# user row, so disabling accounts disables billing too (there would be nobody to
# sell to, and the 4K chip would link to a sign-in route that is not registered).
#
# These addresses are WATCH-ONLY and that is the whole security model: a payment
# is identified by a unique exact amount against one shared receive address, NOT
# by a per-invoice derived address, so this service never holds a private key, a
# seed, or even an xpub. Never put spendable key material in this file — nothing
# here needs it, and the viewer is the internet-facing service.
#
# One 0x address serves EVERY EVM chain (the same address is valid on all of
# them), so funds land in a single wallet with no per-address sweeping.
BILLING_EVM_ADDRESS=
# Comma-separated EVM chains to watch. Adding one later is an edit here, not a
# code change. Supported: ethereum, base, arbitrum, bsc, polygon.
# Prefer cheap L2s first — the user pays the gas, so Base/Arbitrum convert better.
BILLING_EVM_CHAINS=
# USDT-TRC20 receive address (T...). Independent of the EVM rail — either can be
# enabled alone. (No Tron watcher is implemented yet — that is a later phase.)
BILLING_TRON_ADDRESS=

# Optional per-chain RPC override, e.g. BILLING_EVM_RPC_BASE=https://... . Without
# one, a free public endpoint from the built-in table is used, which is fine for
# low volume but rate-limited. Point these at a paid provider before relying on
# them for real money.
# BILLING_EVM_RPC_BASE=
# BILLING_EVM_RPC_BSC=

# How often to scan each chain, in seconds. 0 disables the poller entirely:
# invoices can still be created, they just never settle.
BILLING_POLL_INTERVAL_SEC=30
# How long an invoice stays payable. Deliberately short — a pending invoice holds
# a reserved amount, and there are only 10,000 of them per price.
BILLING_INVOICE_TTL_MIN=30

# Prices in US cents. Every rail settles in a USD stablecoin, so these are also
# the token amount (no exchange rate is involved, and none can move mid-payment).
BILLING_PASS30_USD_CENTS=200
BILLING_PASS365_USD_CENTS=1500
BILLING_TITLE_USD_CENTS=100
# Display only — shows an approximate dong price beside the USD one. Never
# affects what is actually charged.
BILLING_VND_PER_USD=26000

# --- Subtitle providers (user-contributed subtitles on the watch page) ---
# A SIGNED-IN visitor can search these from the watch page, apply a result to
# their own playback, and explicitly save it to the shared catalog so every
# visitor sees it. Anonymous visitors see the panel but it is inert.
#
# These are the SAME keys the admin uses: one upstream account, one daily
# download quota. That is why the caps below exist — here the quota is reachable
# by any signed-in visitor, not just one operator.
#
# Leave BOTH keys blank to turn the feature off entirely: the routes are not
# registered and no panel is rendered. That is the clean rollback. It also
# requires Google sign-in to be configured (see above) — with accounts off there
# would be nobody to offer it to.
# OPENSUBTITLES_API_KEY=
# OPENSUBTITLES_USER_AGENT=phimtor2 v1.0
# Optional: an OpenSubtitles login only raises the shared account's daily quota.
# OPENSUBTITLES_USERNAME=
# OPENSUBTITLES_PASSWORD=
# SUBSOURCE_API_KEY=
# SUBSOURCE_USER_AGENT=phimtor2 v1.0
# Per-user caps on the provider proxy (0 disables a cap). Searching is cheap;
# downloading spends the shared quota, so it is capped tighter and again overall.
# SUBTITLE_SEARCH_PER_HOUR=30
# SUBTITLE_DOWNLOADS_PER_DAY=10
# SUBTITLE_DOWNLOADS_GLOBAL_PER_DAY=150

# --- Subtitle storage (must point at the SAME storage admin writes to) ---
# Mostly read — the admin writes the catalog's subtitles — but the viewer WRITES
# here too when a signed-in user saves one for everyone, so it must be writable.
SUBTITLE_STORAGE_BACKEND=local
# For local: this MUST be the same directory the admin writes subtitles to
# (a shared volume under compose), and writable by this process.
SUBTITLE_STORAGE_DIR=./data/subtitles
# For s3: set the same bucket the admin uses. Secrets are env-only.
# S3_ENDPOINT=
# S3_REGION=us-east-1
# S3_BUCKET=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_USE_SSL=true
